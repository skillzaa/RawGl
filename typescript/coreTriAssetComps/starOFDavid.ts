// import Engine from "../core/rawGl.js";
// import Tri from "../coreComps/coreTri.js";

// export default function starOfDavid (engine :Engine,x:number=50,y:number=50,width:number=30,height:number=20,r:number=0,g:number=0,b:number=0){

//     const tri = new Tri(
//         x,y,   
//         x + width,   y,
//         x + width,y +height,
//         r,g,b);
        
//     tri.addTriangle(
//         x + width,y+height,
//         x , y+height,
//         x,y
//         );
    
// tri.init(engine);
// return tri;     
// }

// // engine.clear(0,0,0.2);

// // tri.draw(engine.gl());